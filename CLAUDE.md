@AGENTS.md

<!--
Intentionally a one-line import.

AGENTS.md is the single source of truth for this project's agent
instructions: Codex reads AGENTS.md, and Claude Code reads CLAUDE.md but
NOT AGENTS.md (https://code.claude.com/docs/en/memory#agents-md), so this
file exists only to point Claude Code at the same content.

Edit AGENTS.md, never this file. Claude Code-specific instructions, if any
are ever needed, go below the import above.

This comment is a block-level HTML comment, which Claude Code strips before
loading the file, so it costs no context.
-->
